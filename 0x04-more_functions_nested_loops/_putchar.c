#include <stdio.h>

/**
 * _putchar - writes the character c to standout
 * @char: the character to print
 * Return: 1 on success
 * on error, -1 is returned, and no err is set to appropraite
 */
int _putchar(char)
{
	return (write(1, &c, 1));
}
