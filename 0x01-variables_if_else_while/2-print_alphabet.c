#include <stdio.h>
/**
 * main - prints the alphabetin lowercase
 * Return: Always
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc < = 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
